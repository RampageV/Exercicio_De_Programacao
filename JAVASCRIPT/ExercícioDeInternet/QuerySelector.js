function NomeCompleto() {

    // Pegando o valor que foi colocado no id="nome"
    var nome = document.querySelector('#nome').value;
    // Mostrando na tela o typeof nome onde está o id="LocalDoNome"
    document.getElementById('LocalDoNome').innerHTML = `${"oi"}`;

    ``
}